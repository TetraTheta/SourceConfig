// SM Autoexec v001

// Complete collection of autoexec
// I do not care about non-existing config even though they will print error message :)

// Map Labs
fov_desired "90"

// MMod HL2
exec nodegraph_fix.cfg

// MMod CM HL2
exec cm_specific_optimizations.cfg

// My own custom configs
exec custom.cfg
exec mmod_custom.cfg
exec skill_manifest.cfg

// Set video mode only in game start
mat_setvideomode 1920 1080 0
